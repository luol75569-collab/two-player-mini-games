# Changelog

## v0.2.1 · 五子棋刷新后落子修复

- 修复刷新恢复五子棋时隐藏页面先测量 canvas，导致棋盘显示尺寸与点击坐标不一致的问题。
- 点击坐标现在会统一按当前可见尺寸换算，并处理棋盘容器旋转和 canvas 边框；移动端、桌面端和高 DPR 屏幕均保持落在点击的准确格子。
- 修复手机端棋盘阻塞页面纵向滚动的问题：滑动和多指操作不落子，只有轻点抬起才提交落子；状态同步也不再把当前页面强制滚回顶部。
- 将根页面横向溢出裁剪改为 `overflow-x: clip`，避免 `html/body` 形成隐式滚动容器，恢复手机端正常纵向触摸滚动。

## v0.2.0 · 断线恢复

- 为每位玩家增加仅本人持有的随机恢复凭证，刷新、切换微信或短暂断网后可恢复原席位。
- 临时断线默认保留房间、棋盘、轮次、汤主身份和提问记录 5 分钟；断线期间暂停改变对局的操作。
- 防止旧 WebSocket 的延迟 `close` 清掉新连接；重复恢复不会新增玩家席位。
- 前端增加连接建立超时、应用层心跳、带抖动的退避重试，以及 `online`、`visibilitychange`、`pageshow` 恢复探测。
- 增加恢复失败、房间到期和服务重启后的明确提示；仅在服务端明确返回失效/不存在时清理本地恢复信息。
- 房间成功创建、加入或恢复时按收件人确认当前有效凭证；旧标签页被接管时收到 `session_replaced` 并停止抢回。
- 恢复请求和心跳都有受控超时，恢复失败但连接仍健康时仍可直接重新创建或加入房间。
- 增加断线恢复、错误凭证、保留席位、海龟汤待回答阶段和到期回收测试。

### 已知部署边界

恢复状态仍是单个 Node 进程内存状态，不能跨进程重启或 Vercel 多实例共享。稳定公网部署需要常驻单进程 Node 服务；多实例部署还需要共享存储、锁和消息分发。

## v0.1.0 · 原始基线

- 原始双人小游戏站发布基线：海龟汤、15×15 五子棋、服务端权威规则和移动端手绘界面。
- 当时的断线重连仍按房间号重新加入，未保留玩家会话和对局状态。
