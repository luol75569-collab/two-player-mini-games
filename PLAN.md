# 双人小游戏站计划书

## 目标
先做一个完全本地运行、手机浏览器可访问的双人小游戏站，首批包含：

1. **海龟汤**：一人当汤主，一人提问，汤主用“是 / 否 / 无关 / 换个问法”回答。
2. **五子棋**：15×15 棋盘，双方轮流落子，自动判断胜负。

本阶段不部署公网服务器，不接数据库；重启服务后房间和对局可以清空。

## 使用方式

- 电脑启动 Node.js 服务后访问 `http://localhost:3000`。
- 手机和电脑连接同一个 Wi-Fi 后，手机访问电脑局域网地址，例如 `http://192.168.1.100:3000`。
- 一人点击“创建房间”，另一人输入 4 位房间号加入。
- 房间内选择“海龟汤”或“五子棋”。

## 技术方案

- 后端：Node.js + 原生 `http` + `ws` WebSocket，尽量少依赖。
- 前端：原生 HTML/CSS/JavaScript，移动端优先，无构建步骤。
- 数据：内存房间；海龟汤题库存为 `soups.json`。
- 服务端监听 `0.0.0.0`，端口读取 `PORT`，默认 3000。

## 目录结构

```text
game-hub/
├── PLAN.md
├── package.json
├── server.js
├── soups.json
└── public/
    ├── index.html
    ├── style.css
    ├── app.js
    ├── manifest.webmanifest
    └── icon.svg
```

## 房间与通信协议

每个房间最多 2 人，服务端生成 4 位数字房间号，并给客户端分配 playerId。前端通过 WebSocket 发送 JSON 消息，服务端校验身份和轮次后广播事件。

基础消息：

- `create_room`：创建房间。
- `join_room`：按房间号加入。
- `select_game`：选择 `soup` 或 `gomoku`。
- `restart`：重新开始当前游戏。
- `error`、`state`、`room_update`：状态与错误反馈。

五子棋消息：

- `gomoku_move {x, y}`：落子。
- 服务端保存棋盘、当前回合和棋子颜色，广播 `gomoku_state`。
- 服务端判断五连、和棋、非法落子，不能信任客户端胜负结果。

海龟汤消息：

- `soup_start`：汤主选择或随机抽题；汤面发送给全员，汤底只发送给汤主。
- `soup_question {text}`：猜题者提问，广播到聊天记录。
- `soup_answer {answer}`：汤主从 `yes/no/irrelevant/rephrase` 中选择，广播回答。
- `soup_guess {text}`：猜题者提交完整推理，汤主选择通过或继续猜。
- `soup_reveal`：汤主揭晓汤底，或猜题者放弃。
- `soup_swap`：交换角色并开始新汤。

汤底绝不发送给猜题者，也不直接写入猜题者的初始页面数据。

## 海龟汤题库

首版内置至少 20 道中文题目，每道包含：`title`、`surface`（汤面）、`answer`（汤底）、可选 `hints`。题库应包含经典题和原创轻量题，适合两个人晚上玩；界面默认不展示答案，汤主点击开始后才可查看。

## 页面设计

### 大厅

- 标题与简短说明。
- “创建房间”按钮。
- 房间号输入框与“加入房间”按钮。
- 显示本地访问提示。

### 房间页

- 显示房间号、在线人数、复制房间号按钮。
- 两个游戏卡片：海龟汤、五子棋。
- 等待第二名玩家加入时显示提示。

### 五子棋页

- 手机宽度自适应的 15×15 棋盘，触摸点击落子。
- 黑白双方标识、轮到谁、胜负提示。
- “再来一局”“返回房间”按钮。

### 海龟汤页

- 汤面卡片。
- 聊天气泡区，区分提问、回答和系统消息。
- 猜题者：问题输入框、提问、提交最终猜测、放弃。
- 汤主：底牌抽屉/卡片和四个快捷回答按钮。
- 揭晓后显示汤底，支持交换角色再来一局。

## 移动端要求

- 添加 viewport，按钮和点击区域至少 44px。
- 同时支持触摸和鼠标，不依赖 hover。
- 防止棋盘页面误滚动和双击缩放。
- 深色但清晰的视觉风格，中文字体优先使用系统字体。
- 断线后显示提示并尝试重连；至少保留当前房间号。
- 不引入外部 CDN，离线启动也能打开页面。

## 验收标准

1. `npm install` 后 `npm start` 能启动。
2. `curl http://127.0.0.1:3000` 返回首页。
3. 两个浏览器标签页可以创建/加入同一房间。
4. 五子棋能同步落子、拒绝越权/重复落子、正确判断五连。
5. 海龟汤能正确隐藏汤底、同步提问回答、揭晓和换角色。
6. 手机访问局域网地址时布局正常，按钮可操作。
7. `node --check server.js` 和前端基本语法检查通过。
