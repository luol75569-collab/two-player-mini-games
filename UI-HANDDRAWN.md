# 手绘风 UI 改造计划书

## 目标

把 `game-hub` 双人小游戏站的整体视觉从「深色卡片风」改造为「手绘涂鸦 / 笔记本纸张风」，营造课间在草稿纸上玩游戏的感觉。**只改视觉，不改任何游戏逻辑、WebSocket 协议和后端代码。**

## 硬性约束

1. **不允许引入外部 CDN / 网络字体**：离线启动也必须正常显示。手写字体只能使用系统字体栈，例如：
   `"Xingkai SC", "HanziPen SC", "Yuanti SC", "Comic Sans MS", "Chalkboard SE", cursive, sans-serif`
   （按平台自动回退，iOS 上会用圆体/行楷类字体。）
2. 只改动 `public/` 下的文件：`style.css`（重写为主）、`index.html`（可增删装饰性元素和 class）、`app.js`（只允许极小的渲染适配，禁止改消息协议与游戏流程）。
3. 移动端优先，按钮可点击区域仍 ≥44px，文字对比度保证可读。
4. 五子棋棋盘改为「方格纸 + 手绘棋子」风格，触摸落子逻辑不变。

## 视觉方向

### 整体氛围：草稿纸笔记本

- **背景**：米白/牛皮纸色（如 `#f5efe0` / `#f7f1e3`），叠加手绘感纹理。纹理用内联 SVG 或 CSS 实现（细网格线模拟方格纸 + 轻微噪点），不能用外链图片。
- **配色**：铅笔灰 `#3d3d3d`（正文）、马克笔蓝 `#4a7de0`、蜡笔红 `#e0604f`、荧光黄 `#f2c94c`、草绿 `#6aab6e`。海龟汤/五子棋两个主题卡可用不同马克笔色区分。
- **深色模式不做**，本次只有纸张亮色主题（若实现方便可保留 prefers-color-scheme 兜底，非必须）。

### 手绘感的关键技法（CSS 实现）

1. **不规则边框**：卡片、按钮用不对称 `border-radius`（如 `255px 15px 225px 15px / 15px 225px 15px 255px` 这类“手绘抖动圆角”），配 2px 实线描边，颜色用铅笔灰而不是纯黑。
2. **微旋转**：卡片和标题随机 `rotate(-0.6deg ~ 0.8deg)`，像贴在纸上的便签。注意旋转幅度小，不影响阅读和点击。
3. **涂鸦阴影**：`box-shadow: 2px 3px 0 rgba(61,61,61,0.15)` 这种硬偏移阴影，模拟铅笔蹭出的层次，不用柔和弥散阴影。
4. **胶带/图钉装饰**：重要卡片（汤面卡、房间号卡）顶部加一个半透明的“胶带条”或图钉 emoji/SVG，像贴在笔记本上。
5. **按钮**：手绘描边 + 按下时 `translate(2px,3px)` 阴影归零的“按下去”反馈。
6. **虚线与下划线**：标题用手绘波浪下划线（`text-decoration: wavy underline` 或 SVG 曲线），分隔线用不规则虚线。
7. **聊天气泡**：提问/回答气泡改成对话框手绘风，回答标签（是/否/无关/换个问法）做成彩色“盖章/贴纸”样式，四色区分。
8. **涂鸦小装饰**：页面空白处点缀少量内联 SVG 涂鸦（星星、箭头、螺旋线、小乌龟🐢轮廓、棋子），数量克制，不能影响性能和小屏布局。

### 各页面改造点

- **大厅**：标题写成“大字报”手绘标题 + 波浪下划线；创建/加入按钮做成两种颜色的马克笔按钮；加纸胶带装饰。
- **房间页**：房间号做成“便利贴”样式，四位数字放大手写体。
- **五子棋**：
  - 棋盘底色改方格纸米色，网格线用铅笔灰细线；
  - 棋子改为手绘圆圈风格：黑子 = 涂黑圆 + 不规则白边高光，白子 = 白圆 + 铅笔描边；
  - 胜利连线改成红色蜡笔粗线；
  - 最后一手标记改成小红圈。
  - 棋盘的触摸判定、坐标换算逻辑不许改，只改绘制样式。
- **海龟汤**：
  - 汤面卡 = 贴在纸上的便签（微旋转 + 胶带）；
  - 「查看汤底」按钮保留，汤底展开区做成“刮开/翻开”的黄色高亮块（马克笔涂抹背景）；
  - 快捷回答四按钮做成四色印章贴纸；
  - 揭晓汤底时给一个手绘庆祝装饰（如彩带 SVG 或 🎉 标题）。

## 验收标准

1. 刷新页面即可看到新风格（静态文件，无需重启服务器）。
2. `node --check public/app.js` 通过（若有改动）。
3. 电脑浏览器和手机宽度（375px）下：所有按钮可点、文字可读、无横向滚动条。
4. 两个标签页完整跑通一局海龟汤和一局五子棋，功能与改造前一致。
5. 断网状态下打开页面，字体与纹理依然正常（验证无外部资源依赖）。
